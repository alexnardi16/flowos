import { SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import { Card, Chip, palette } from '@/components/ui';
import { useFlowStore } from '@/lib/store';
export default function Plan(){const items=useFlowStore(s=>s.commitments.filter(c=>c.status!=='done'));return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.wrap}><Text style={s.title}>Piano</Text><Text style={s.sub}>Impegni fissi e blocchi IA, in un’unica sequenza.</Text>{items.map(c=><Card key={c.id}><View style={s.row}><Chip>{c.fixed?'FISSO':'IA'}</Chip><Text style={s.time}>{c.scheduledAt?new Date(c.scheduledAt).toLocaleTimeString('it-IT',{hour:'2-digit',minute:'2-digit'}):'Flessibile'}</Text></View><Text style={s.item}>{c.title}</Text><Text style={s.meta}>{c.durationMinutes} min · {c.context} · energia {c.energy}</Text></Card>)}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:palette.bg},wrap:{padding:20,paddingBottom:110,gap:14},title:{fontSize:31,fontWeight:'900',color:palette.ink,marginTop:14},sub:{fontSize:16,color:palette.muted,marginBottom:8},row:{flexDirection:'row',justifyContent:'space-between',alignItems:'center'},time:{fontWeight:'800',color:palette.primary},item:{fontSize:18,fontWeight:'800',color:palette.ink,marginTop:12},meta:{fontSize:13,color:palette.muted,marginTop:6}})
