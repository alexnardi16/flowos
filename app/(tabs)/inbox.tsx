import { SafeAreaView, ScrollView, StyleSheet, Text } from 'react-native';
import { Card, palette } from '@/components/ui';
import { useFlowStore } from '@/lib/store';
export default function Inbox(){const items=useFlowStore(s=>s.commitments.filter(c=>c.status!=='done'&&c.confidence<.85));return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.wrap}><Text style={s.title}>Inbox</Text><Text style={s.sub}>Solo ciò che richiede una tua decisione.</Text>{items.length?items.map(c=><Card key={c.id}><Text style={s.item}>{c.title}</Text><Text style={s.meta}>Confidenza IA {Math.round(c.confidence*100)}% · verifica consigliata</Text></Card>):<Card><Text style={s.empty}>Tutto chiaro. Nessuna decisione in sospeso.</Text></Card>}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1,backgroundColor:palette.bg},wrap:{padding:20,gap:14},title:{fontSize:31,fontWeight:'900',color:palette.ink,marginTop:14},sub:{fontSize:16,color:palette.muted,marginBottom:8},item:{fontSize:18,fontWeight:'800',color:palette.ink},meta:{fontSize:13,color:palette.muted,marginTop:6},empty:{fontWeight:'800',color:palette.success}})
